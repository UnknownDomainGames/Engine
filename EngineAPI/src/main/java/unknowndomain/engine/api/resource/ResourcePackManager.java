package unknowndomain.engine.api.resource;

import java.util.List;

/**
 * Resource packs are sorted with priority in {@link ResourceManager#getResources()}.
 */
public interface ResourcePackManager extends ResourceManager<ResourcePack> {
}
