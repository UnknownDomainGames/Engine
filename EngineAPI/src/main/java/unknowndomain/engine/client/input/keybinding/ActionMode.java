package unknowndomain.engine.client.input.keybinding;

/**
 * Action mode of key input
 *
 */
public enum ActionMode {
  /**
   * A one-time trigger of the key.
   */
  PRESS,
  /**
   * Switch behavior. Press once to activate and press again to deactivate.
   */
  SWITCH;
}
